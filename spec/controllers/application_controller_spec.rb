require 'rails_helper'

RSpec.describe ApplicationController, type: :controller do
  # describe "GET #prepare(merchant, buyer, packages)" do
  #   # these variables are supposed to be Active Record instance objects ):
  #   let(:merchant)
  #   let(:buyer)
  #
  #
  #   it "creates two instances of ActiveShipping::Location" do
  #     original_count = ObjectSpace.each_object(ActiveShipping::Location).count
  #     get :prepare, (merchant, buyer, packages)
  #     new_count = ObjectSpace.each_object(ActiveShipping::Location).count
  #     expect(new_count).to be > original_count
  #     expect(new_count).to eq 2
  #   end
  # end
end
